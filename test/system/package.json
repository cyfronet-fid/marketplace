{
  "name": "e2e",
  "version": "1.0.0",
  "description": "Integration tests",
  "main": "",
  "scripts": {
    "cy:run": "TZ=Europe/Warsaw cypress run --browser chrome --headless --quiet",
    "cy:run-debug": "DEBUG=cypress:* TZ=Europe/Warsaw cypress run --browser chrome --headless --quiet",
    "cy:run-browser": "TZ=Europe/Warsaw cypress run --browser chrome --quiet"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^7.1.0",
    "typescript": "^4.2.3"
  },
  "devDependencies": {
    "@cypress/skip-test": "^2.6.0",
    "@medv/finder": "^2.0.0",
    "@types/node": "^14.14.37",
    "cypress-promise": "^1.1.0",
    "cypress-terminal-report": "^3.0.3",
    "cypress-wait-until": "^1.7.1"
  }
}
