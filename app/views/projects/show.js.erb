{
  let projectSelect = document.getElementById("customizable_project_item_project_id") ??
                      document.getElementById("project_research_product_project_id");
  let child = document.createElement("option")
  child.text = "<%= @project.name %>"
  child.value = <%= @project.id %>
  child.selected = true
  projectSelect.append( child );

  projectSelect.value = "<%= @project.id %>";
  projectSelect.focus();
  projectSelect.dispatchEvent(new Event("change"));

  $("#ajax-modal").modal("hide");
}
