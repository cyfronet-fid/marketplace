.card.bg-gray-5.border-0
  .card-body
    %h3.card-title= link_to service.name.truncate(35, separator: " "), service, name: service.name
    .row.card-description
      .col-4
        = object_logo(service)
      .col
        = service.tagline.truncate(105, separator: " ")
    .clearfix
  .card-footer.no-gutters.row.mb-2
    .col.x-small
      %span
        = _("Organisation") + ":"
      %strong= resource_organisation_text(service)
    .col.x-small.no-gutters
      %span.text-right
        = _("Dedicated for") + ":"
      %strong.text-right{ "data-toggle" => "tooltip",
                          "title" => "#{ service.target_users.map(&:name).join(", ")}" }
        #{service.target_users.map(&:name).join(", ").truncate(26, separator: " ")}
