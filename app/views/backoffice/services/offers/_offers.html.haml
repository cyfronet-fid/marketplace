.card-group.row
  - if offers.count > 1
    = render partial: "backoffice/services/offers/offer", collection: offers, locals: { service: service }
  - elsif offers.count == 1
    %p
      = _("This resource has one default offer.")
  - else
    %p
      = _("This resource has no offers.")
