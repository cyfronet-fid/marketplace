= simple_form_for [:backoffice, platform] do |f|
  = f.input :name

  .btn-group
    = f.button :submit, class: "btn btn-success"
    = back_link_to t("cancel"), platform, prefix: :backoffice, class: "btn btn-secondary"
