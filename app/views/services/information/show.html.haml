- content_for :title, _("Information")
.row{ class: "#{map_view_to_order_type(@offer || @bundle)}" }
  - if @bundle.present?
    = render "services/information/bundle", bundle: @bundle
  - else
    .col-md-5.access-information
      = render Services::InformationDescriptionComponent.new(order_type: map_view_to_order_type(@offer),
                                                               service_title: wizard_title,
                                                               offer: @offer)
    .col-md-7.access-image

- form_url = @service.is_a?(DeployableService) ? deployable_service_information_path(@service) : service_information_path(@service)
= simple_form_for @step, url: form_url, method: :put, html: { id: "order-form" } do
  -# empty block
