- content_for :title, _("Information")
.row{ class: "#{map_view_to_order_type(@offer)}" }
  .col-md-6.access-information.pt-5.vertical-center
    -# TODO: refactor dynamic translation
    = t(".#{map_view_to_order_type(@offer)}.description_html",
        service_title: wizard_title, faq_url: help_path)
    - webpage = @offer.order_url || @offer.webpage
    - unless (@offer.order_required? && !@offer.external) || webpage.blank?
      = link_to t("common.#{map_view_to_order_type(@offer)}.link"),  webpage,
          class: "btn btn-outline-primary mt-3", target: "_blank", title: "External link"
      -# TODO: refactor dynamic translation (above)
  .col-md-6.access-image

= form_for @step, url: service_information_path(@service), method: :put, html: { id: "order-form" } do
  -# empty block
