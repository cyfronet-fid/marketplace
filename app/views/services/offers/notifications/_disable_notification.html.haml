= turbo_frame_tag "#{dom_id(offer)}-action-button" do
  = link_to service_offer_notification_path(offer.service.id, offer.iid,
  notification_request.id ), data: { turbo_method: :delete } do
    %span.btn.btn-secondary.font-weight-bolder
      = _("Disable availability notifications")
