= field.input :id,
  label: attr.label,
  input_html: { value: "#{attr.value}",
    name: "#{field.object_name}[#{attr.id}]",
    "data-date-format" => to_js_date_format(attr.date_format) },
  error: attr.errors.messages[:id].join(" "),
  hint: attr.description,
  as: :date_time_picker
