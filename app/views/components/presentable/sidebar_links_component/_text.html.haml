%ul.list-group
- fields.each do |field|
  - if object.send(field).present?
    %li.list-group-item
      %span
        -# TODO: refactor dynamic translation
        = t(".fields.#{field}.#{object.send(field)}")
