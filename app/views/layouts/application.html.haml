!!!
%html{ lang: "en", class: "#{Rails.env}" }
  = render "layouts/head"
  .spinner-background
    .spinner
      %div
      %div
      %div
      %div
  %body{ class: ("main" if current_page?("/")) }
    - if current_page?("/")
      .main-elements
    - if ENV["MP_INSTANCE"]
      = render "layouts/badge"
    = render "layouts/flash"
    %header
      = render "layouts/navbar",
        categories: @siblings || @root_categories
    %main
      = render "layouts/breadcrumb"
      = yield
  = render "layouts/report", report: @report
  = render "layouts/modal"
  = render "layouts/footer", root_categories: @root_categories
