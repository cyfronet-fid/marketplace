#!/usr/bin/env ruby
APP_ROOT = File.expand_path("..", __dir__)

require_relative "../config/boot"
require_relative "../config/environment"

Jms::Subscriber.new(ENV.fetch("STOMP_CONFIG_FILEPATH", nil)).run
